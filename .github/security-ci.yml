schema_version: 1
source_ci_workflow: CI
default_branch: main

required_checks:
  - name: "CI / check"
    source_job: check

advisory_checks: []

fail_closing_manual_checks:
  - name: "CI / gitleaks"
    source_job: gitleaks
    tool: gitleaks

pins:
  gitleaks:
    release_tag: "v8.30.1"
    archive_version: "8.30.1"
    checksum_sha256: "551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb"
    platform: linux_x64

report_limits:
  max_rows_per_summary: 30
  redact_secrets: true
